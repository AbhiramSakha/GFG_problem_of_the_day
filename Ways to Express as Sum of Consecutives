class Solution:
    def getCount(self, n):
        count = 0
        k = 2

        while k * (k + 1) // 2 <= n:
            temp = n - k * (k - 1) // 2
            if temp > 0 and temp % k == 0:
                count += 1
            k += 1

        return count
