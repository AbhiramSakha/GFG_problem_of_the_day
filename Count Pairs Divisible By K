class Solution:
    def countKdivPairs(self, arr, k):
        freq = [0] * k
        count = 0

        for num in arr:
            rem = num % k
            complement = (k - rem) % k
            count += freq[complement]
            freq[rem] += 1

        return count
